============
Installation
============

At the command line::

    $ easy_install django-collectform

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-collectform
    $ pip install django-collectform
